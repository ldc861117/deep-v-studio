[pytest]
testpaths = tests
# Provider tests are self-contained and don't need torch.
# Use --import-mode=importlib so pytest doesn't trigger ltx_pipelines/__init__.py
# when collecting provider tests.
addopts = --import-mode=importlib
